import React, { FC } from 'react'

import { Container } from '../../components/container'
import { Text } from 'react-native'

const TransactionDetailScreen: FC = () => {
    return (
        <Container>
            <Text>Detail</Text>

        </Container>
    )
}

export default TransactionDetailScreen